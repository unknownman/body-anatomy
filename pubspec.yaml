name: body_anatomy_viewer
description: A Flutter package for rendering human body anatomy with dynamic muscle highlighting using CustomPainter.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
